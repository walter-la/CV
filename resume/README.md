# Personal Resume

This project contains a simple personal resume website built with **React**, **TypeScript**, **Tailwind CSS**, and **Vite**.

## Development

Install dependencies and start the development server:

```bash
npm install
npm run dev
```

## Build

To create a production build:

```bash
npm run build
```

The site can be deployed on platforms like GitHub Pages.
